function motLePlusCourt(phrase) {
	let petitmot = "";

  for (let i = 0; i < phrase.length; i++) {
    if (phrase < petitmot) {
    phrase[i] == petitmot;
    } console.log(petitmot)
    
  }
}

console.log(motLePlusCourt("Bonjour à tous et bienvenue")); // Résultat attendu : "à"